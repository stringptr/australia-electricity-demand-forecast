FROM python:3.12-slim

RUN pip install --no-cache-dir \
    "dlt[postgres]" \
    httpx \
    openelectricity \
    boto3

WORKDIR /app

ENV PYTHONUNBUFFERED=1
ENV DLT_PROJECT_DIR=/app

ENTRYPOINT ["python"]
